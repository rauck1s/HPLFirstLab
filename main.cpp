#include <iostream>
#include "vector"
#include "DynamicArray.h"
int main() {
    DynamicArray test;
    std::cout << test.getSize();
    for(int i = 0; i < test.getSize(); i++){
        std::cout << test
}